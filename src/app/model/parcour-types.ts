export enum ParcourElementRotation{
    UP, LEFT, DOWN, RIGHT
};

export enum CurveTurn {
    LEFT, RIGHT
};

export type CurveLength = 1 | 3 | 4;

export type StraightLength = 3 | 4;

export enum Lane {
    LEFT, RIGHT
};